# Aggregate Evaluation Results

This directory contains **publication-safe aggregate outputs** from the final MHFSafeguard model evaluation workflow.

Planned/current result artefacts include:

```text
transformer_full_eval.txt
```

The final paper compares traditional machine-learning, neural deep-learning, and transformer models on the same **957,154-sentence actual-only held-out test set**.

Do not commit raw MentalHealthForum.net text, SQLite databases, row-level misclassification exports, or other outputs that reproduce sensitive forum content. Aggregate metrics and execution logs that contain no raw user text are suitable for this public directory.
